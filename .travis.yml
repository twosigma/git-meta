language: node_js
node_js:
  - "6.1.0"

env:
  - CC=gcc-5
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-5
      - g++-5

before_install:
     -  cd node

install:
      - npm install

script:
      - npm test

notifications:
  email: false
